{
  "exported_at": "2026-05-20",
  "task_count": 12,
  "tasks": [
    {
      "id": 71,
      "subject": "Migrate SPA to Alpine.js",
      "status": "pending",
      "description": "Final SPA task. After all other features land, retrofit existing vanilla-JS render functions (renderConversations, renderThread, renderAnnounces, renderPaths, modals) to Alpine.js. Retire `withPreservedState`, the manual `el()` factory, and the wipe-and-rebuild pattern. Ship Alpine inline in the SPA blob; ~15 KB gzipped cost. See [[spa-alpine-js]] memory for rationale."
    },
    {
      "id": 99,
      "subject": "Network topology view: graph of seen nodes",
      "status": "pending",
      "description": "New tab in the SPA that shows a visual graph of nodes we can see (us in the middle, heard peers around). Use SVG, force-directed or circular layout. Data: /api/announces + /api/paths. Show edges with RSSI/SNR if known. Click a node to open a conversation with it. Useful for at-a-glance 'who's on the mesh right now'. Sizes ~1-2 days of SPA work."
    },
    {
      "id": 135,
      "subject": "Power: OLED idle-off with button wake",
      "status": "pending",
      "description": "After N seconds (default 60s) of no display update and no button press, call ST7789::displayOff(). Wake on:\n - button press (short or long)\n - new inbound LXMF message (already triggers a display update)\n - explicit /api/display/wake endpoint for the SPA to flick on remotely\n\nDisplay task already runs in its own FreeRTOS task — adding an idle timer is local.\nSaves ~15 mA when the user isn't looking at the device. Persists across reboots via the existing connectivity/UI prefs file."
    },
    {
      "id": 137,
      "subject": "Power: ESP32-S3 light-sleep with radio IRQ wake",
      "status": "pending",
      "description": "Configure esp_pm_config_esp32s3_t with min_freq_mhz=80, max_freq_mhz=240, light_sleep_enable=true. Wakes on radio DIO1 (RX_DONE), WiFi rx, FreeRTOS timer, button.\n\nSave ~70 mA on top of modem-sleep (#134). Higher risk than the other power tasks: latency-sensitive paths (Resource REQ → reply within window, Link keepalives, KISS host responses) may need explicit ESP_PM_CPU_FREQ_MAX wake-locks. Wants a sweep through the radio receive path and Resource state machine.\n\nPrerequisite: #133 (so we can measure the win) + #134 (modem-sleep landed). Test plan: a full bidirectional Resource transfer with light-sleep on; confirm no part_timeout / WDT events."
    },
    {
      "id": 138,
      "subject": "Power (low priority): adaptive TX power from RSSI history",
      "status": "pending",
      "description": "Per-peer link-budget tracking: use the worst recent RSSI of the active peer set from the AnnounceLog to lower-bound TX power at link_budget + 6 dB margin.\n\nHalves TX current during sends. Not steady-state savings — TX windows are short — but cumulatively meaningful in active conversations.\n\nLow priority because the gain is small relative to the four tasks above, and the implementation needs care (changing TX power per-packet vs per-peer; interaction with regulatory cap)."
    },
    {
      "id": 140,
      "subject": "Power: user-controlled BLE radio toggle",
      "status": "pending",
      "description": "A Connectivity-tab switch that runtime-toggles the ESP32 BLE controller.\n\nWhen OFF: SerialBT.end() + esp_bt_controller_disable(). Reclaims ~70 KB RAM and stops the BLE radio modem. Saves the BLE controller's idle current draw — measure delta against #133's slope to quantify.\n\nWhen ON: bt_start() bring-up the existing SerialBT BLEUart path so phones can connect via KISS-over-BLE. Mirrors today's behaviour.\n\nPersists the user's last setting across reboots. Default: ON (today's implicit default since SerialBT_init = false at boot and bt_start is triggered by KISS host command — preserve the workflow).\n\nSurface the current state on the Connectivity tab + a pill on the status popover so users can see whether the radio is currently up.\n\nBuilt on top of the existing Bluetooth.h state machine — no new BLE stack work."
    },
    {
      "id": 141,
      "subject": "Power: re-approach WiFi modem-sleep with retry-aware UX",
      "status": "pending",
      "description": "Deferred from #134. WiFi.setSleep(WIFI_PS_MIN_MODEM) saves ~70 mA at idle but introduced user-visible 'connection reset by peer' on the first request after a long idle gap — visible in the SPA and breaks the test suite. Disabled in Remote.h with WIFI_PS_NONE for now.\n\nPlan for the retry:\n - Investigate whether WIFI_PS_MIN_MODEM specifically is causing the wake latency, or whether it's an arduino-esp32 known issue with WiFi.setSleep + STA mode (some firmware combos drop ARP entries).\n - Try DTIM 1 vs 3 vs 10 — longer DTIM = deeper sleep but worse latency.\n - SPA: make the `_req` transport layer retry idempotent GET / POST after a single connection-reset (one-shot). Adds robustness even without sleep.\n - Test suite: add session-level retry adapter so individual tests don't see the first wake.\n - Only re-land setSleep after both retry paths exist and we've measured no regression in user-visible latency.\n\nValidate with #133 slope before / after."
    },
    {
      "id": 162,
      "subject": "Browser desktop notifications for incoming messages",
      "status": "pending",
      "description": "Add Web Notifications API support to the SPA so users get a desktop / mobile-OS notification when an LXMF message arrives while the tab is backgrounded or the device is locked.\n\nScope:\n- Request Notification.permission on first WS `incoming` event after the user has interacted with the SPA (not on load — browsers throttle unsolicited prompts).\n- Fire a Notification with sender display name + message preview when an incoming message arrives AND document.visibilityState !== 'visible' (foregrounded chats already update inline; firing a system toast in that case would just be noise).\n- Tap on the notification refocuses the SPA tab and opens the conversation for that peer.\n- Provide a per-user preference toggle in Settings → Identity (alongside 'Persist outbound attachments' etc) so users can disable the noise.\n- Persist the preference in localStorage (matches `state.prefs.autoDownloadAttachments` pattern).\n- Coalesce: if multiple messages from the same peer arrive within a short window, replace the previous notification rather than stack up N system toasts (use the `tag` field on Notification).\n- PWA-installed mode: the same notification path should work when launched as an installed app on Android/iOS; iOS Safari needs the SPA installed to home-screen for notifications to fire at all, so document this in the toggle's description.\n\nOut of scope: server-side push (Web Push API) for fully-backgrounded 'tab closed' delivery — that requires a push service the device can't talk to without internet."
    },
    {
      "id": 163,
      "subject": "rmap.world v4 interface-discovery announce",
      "status": "pending",
      "description": "Make the device auto-discoverable by https://rmap.world v4 by emitting an RNS `rnstransport.discovery.interface` aspect announce. NOT MQTT — it's a standard Reticulum announce that the upstream Python `rns` ≥1.1 library produces when `discoverable = yes` is set on an interface; the C++ microReticulum port doesn't implement this at all (`grep` for `discoverable` returns zero hits).\n\nPlan:\n1. Read upstream Python RNS source to determine the exact wire format of the discovery announce app_data — file paths likely `RNS/Interfaces/Interface.py` and the announce-builder in `RNS/Transport.py`. Clone the upstream Reticulum repo locally as a sibling sister to our existing `microReticulum/` clone if it isn't already.\n2. Port the announce emitter into microReticulum on the `rnstransport.discovery.interface` aspect. Reuse our existing announce path; the new aspect just needs a fresh destination + a periodic timer (default 6h per the upstream default).\n3. Plumb the fields we already have:\n   - latitude/longitude/height — from `Web::Gps::last_fix()` (we already collect this); height is unused for now (could come from GPS altitude later).\n   - radio params — `lora_freq`, `lora_bw`, `lora_sf`, `lora_cr`, `lora_txp` are already in scope.\n   - discovery_name — default to the existing RNode-style name (`RNode <xxxx>` derived from MAC; same pattern as `bt_devname`). User-overridable via the backend.\n   - announce_interval — user-configurable in the backend, default 360 min.\n   - reachable_on — leave nil for now; we're not a TCP server.\n4. SPA: add a 'RMAP discovery' section to the Connectivity popover (the one anchored to the antenna icon). Surface the discovery_name (editable), the announce interval, the lat/lon being broadcast, and an Enable toggle. Default off — privacy-by-design per rmap's docs ('Only interfaces where you explicitly set discoverable = yes will appear on RMAP v4. Your node's existence on the network is never revealed without your consent.').\n5. Add a `[[RMAP World]]` TCPClientInterface equivalent so the announce reaches rmap.world:4242 directly. Our devices have WiFi, so we don't need a separate transport-node bridge.\n6. Persist the config to `/lxmf/rmap.json` (or similar) so the toggle survives reboots.\n\nOut of scope for v1: dynamic announce-interval adjustment based on whether we have a fix yet; height from GPS altitude; encrypted-discovery / publish_ifac. Default to the rmap-public-anonymous path.\n\nSource-of-truth references:\n- https://rmap.world/info.html — describes the discovery system from the consumer side.\n- https://reticulum.network/manual/interfaces.html#discoverable-interfaces — lists config keys.\n- The upstream RNS Python source (to be cloned locally) — authoritative for the announce wire format."
    },
    {
      "id": 168,
      "subject": "In-firmware diagnostic log ring exposed over HTTP (replace dead serial)",
      "status": "pending",
      "description": "USB CDC serial on T-Beam Supreme returns 0 bytes for both `cat` and pyserial reads — confirmed across multiple sessions. Blocks all serial-based debugging including the Resource spill crash (#167) and any future field issues.\n\nApproach: ring buffer of last N log lines (e.g. 4 KiB) inside the NOTICE/NOTICEF/INFO/INFOF/ERROR/ERRORF/WARNING/WARNINGF macros via a sink hook on RNS::log/logf. Expose via:\n- GET /api/debug/log → tail of ring as text/plain (bearer-auth gated)\n- WS event `debug_log` streaming new lines to connected clients (optional)\n- SPA 'Diagnostics' tab to view live + dump\n\nStorage: in-PSRAM (cheap, big enough for a session's worth of logs). Optional opt-in dump to SD on FATAL/restart for post-crash forensics.\n\nTrigger to build: blocks #167 progress until logs are visible again. Independent of the serial USB CDC issue (which may be an esp-idf USB stack bug we can't fix locally)."
    },
    {
      "id": 169,
      "subject": "WiFi / web-server silent-wedge watchdog",
      "status": "pending",
      "description": "User asked: 'Is there a way we can get the wifi / device to recover automatically when it gets stuck?' Today's behavior: WiFi STA stays 'connected' but AsyncWebServer / TCP stack wedges silently — the device looks alive but stops responding to HTTP, requiring physical power-cycle.\n\nExisting handling:\n- 30 s STA→softAP fallback for *first-attach* failures only (Remote.h:281)\n- 10 s WR_RECONNECT_INTERVAL retries when status leaves WL_CONNECTED\n- No watchdog for 'WiFi connected, AsyncWebServer wedged' case\n\nApproach options (pick one, weigh complexity vs reliability):\n\n1. **Loopback HTTP ping watchdog** — periodic GET /api/ping from a low-prio FreeRTOS task; N consecutive failures (e.g. 3 over 90 s) → WiFi.disconnect()+begin() or esp_restart(). Most reliable; needs HTTPClient + small auth bypass for localhost.\n2. **AsyncWebServer activity counter** — increment a counter in a global request middleware; if WiFi is up, a client is associated/connected, and counter hasn't moved in N minutes, restart the network stack. Lower overhead, more heuristic.\n3. **Hardware TWDT bump** — set ESP32 task watchdog to bite the main loop if it stalls for >M seconds, force reset. Already partially in place via OS::reset_watchdog calls; needs configuration audit.\n\nRecommended: option 1 (loopback ping) as the truth signal — if the device can't HTTP-loopback to itself, something's structurally wrong. Combine with option 3 for hard-stuck protection."
    },
    {
      "id": 175,
      "subject": "LXMFInbox::recent()/since() returns vector by value — copies the whole ring per HTTP fetch",
      "status": "pending",
      "description": "`LXMFInbox::recent(size_t)` and `LXMFInbox::since(uint32_t)` both return `std::vector<MessageRecord>` BY VALUE. Each call to GET /api/identities/{}/{inbox|outbox} or the new /body endpoint's seq lookup copies the entire in-memory ring (up to 50–200 MessageRecords, each with std::string title + attachments vector). At ~100 bytes per record, that's ~10–20 KiB of transient allocation on the default heap (internal SRAM) per HTTP request.\n\nAudit-found during #170 (Pattern B — in-mem duplication via copying). Same anti-pattern: the canonical data lives in `_ring` (std::deque<MessageRecord>), but every accessor returns a fresh copy.\n\nFix options:\n1. Return `const std::deque<MessageRecord>&` + a filter callback (caller iterates without copying).\n2. Replace with an iterator-pair API (begin/end with filter predicates).\n3. If the JSON serializer can be made streaming (chunked emit, one record at a time), even better — no temp vector at all.\n\nThe streaming /body endpoint's seq lookup is the worst offender right now since it calls `recent(static_cast<size_t>(-1))` — full ring copy just to find one record by seq. Fix: add a `find_by_seq(uint32_t)` returning `const MessageRecord*` direct from the deque."
    }
  ]
}
